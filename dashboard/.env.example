# Dashboard Environment Variables
# Copy file này thành .env.local để dùng local

# URL của backend API (Express server)
# Development: để trống → Vite proxy sẽ forward /api → localhost:3000
# Production (Vercel): điền URL backend đang chạy
VITE_API_URL=
